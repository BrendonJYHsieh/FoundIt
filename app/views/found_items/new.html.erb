<!DOCTYPE html>
<html>
  <head>
    <title>Post Found Item - FoundIt</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
        background: #f5f5f5;
        color: #333;
      }

      .container {
        max-width: 800px;
        margin: 40px auto;
        padding: 0 20px;
      }

      h1 {
        text-align: center;
        color: #2E5BBA;
        font-size: 2rem;
        margin-bottom: 10px;
      }

      p.subtitle {
        text-align: center;
        font-size: 1.1rem;
        color: #666;
        margin-bottom: 30px;
      }

      form {
        background: white;
        border-radius: 15px;
        padding: 30px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        border: 1px solid #e0e0e0;
      }

      label {
        display: block;
        margin-bottom: 5px;
        font-weight: bold;
        color: #2E5BBA;
      }

      input, textarea, select {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 8px;
        margin-bottom: 20px;
        font-size: 1rem;
        transition: border 0.2s ease;
      }

      input:focus, textarea:focus, select:focus {
        border-color: #2E5BBA;
        box-shadow: 0 0 0 2px rgba(46, 91, 186, 0.1);
        outline: none;
      }

      .btn {
        display: inline-block;
        background: #2E5BBA;
        color: white;
        padding: 12px 24px;
        border: none;
        border-radius: 50px;
        font-size: 1.1rem;
        font-weight: bold;
        text-decoration: none;
        cursor: pointer;
        transition: all 0.3s ease;
      }

      .btn:hover {
        background: #1E4A9A;
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(46, 91, 186, 0.3);
      }

      .back-link {
        text-align: center;
        margin-top: 25px;
      }

      .back-link a {
        color: #2E5BBA;
        text-decoration: none;
        font-weight: bold;
      }

      .back-link a:hover {
        text-decoration: underline;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <h1>📦 Post a Found Item</h1>
      <p class="subtitle">Help others recover their lost belongings — share details of what you found.</p>

      <%= form_with model: @found_item, url: found_items_path, local: true, multipart: true do |f| %>
        <div>
          <%= f.label :item_type, "Item Type" %>
          <%= f.select :item_type,
            options_for_select(%w[phone laptop textbook id keys wallet backpack other], @found_item.item_type),
            { include_blank: "Select type" },
            required: true %>
        </div>

        <div>
            <%= f.label :description, "Description", for: "found_item_description" %>
            <%= f.text_area :description,
                id: "found_item_description",
                rows: 4,
                minlength: 10,
                maxlength: 500,
                placeholder: "e.g., Black iPhone with a clear case and 'Columbia' sticker on the back",
                required: true %>
        </div>      

        <div>
          <%= f.label :location, "Location Found" %>
          <%= f.text_field :location, placeholder: "e.g., Lerner Hall entrance", required: true %>
        </div>

        <div>
          <%= f.label :found_date, "Date Found" %>
          <%= f.date_field :found_date, required: true %>
        </div>

        <div>
          <%= f.label :photos, "Upload Photos (optional)" %>
          <%= f.file_field :photos, 
              multiple: true,
              accept: "image/*",
              class: "photo-upload-input" %>
          <small class="help-text">
            💡 <strong>Tips:</strong> You can select multiple photos at once. Supported formats: JPG, PNG, GIF, WebP
          </small>
          <div id="photo-preview" style="margin-top: 10px;"></div>
        </div>

        <div style="text-align:center;">
          <%= f.submit "Submit Found Item", class: "btn" %>
        </div>
      <% end %>

      <div class="back-link">
        <%= link_to "← Back to Dashboard", dashboard_path %>
      </div>
    </div>

    <script>
    document.addEventListener('DOMContentLoaded', function() {
      const photoInput = document.querySelector('.photo-urls-input');
      const previewContainer = document.getElementById('photo-preview');
      
      if (photoInput && previewContainer) {
        photoInput.addEventListener('input', function() {
          const urls = this.value.split(/[\n,]+/).map(url => url.trim()).filter(url => url);
          
          previewContainer.innerHTML = '';
          
          urls.forEach((url, index) => {
            if (url) {
              const img = document.createElement('img');
              img.src = url;
              img.style.maxWidth = '100px';
              img.style.maxHeight = '100px';
              img.style.margin = '5px';
              img.style.border = '1px solid #ddd';
              img.style.borderRadius = '4px';
              img.onerror = function() {
                this.style.border = '2px solid red';
                this.alt = 'Failed to load';
              };
              previewContainer.appendChild(img);
            }
          });
        });
      }
    });
    </script>
  </body>
</html>