<!DOCTYPE html>
<html>
  <head>
    <title><%= @lost_item.item_type.capitalize %> Lost - FoundIt</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
        background: #f5f5f5;
        color: #333;
      }

      .container {
        max-width: 900px;
        margin: 40px auto;
        padding: 0 20px;
      }

      h1 {
        text-align: center;
        color: #2E5BBA;
        font-size: 2rem;
        margin-bottom: 30px;
      }

      .card {
        background: white;
        border-radius: 15px;
        padding: 30px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        border: 1px solid #e0e0e0;
      }

      .status {
        display: inline-block;
        padding: 6px 14px;
        border-radius: 20px;
        font-weight: bold;
        margin-bottom: 20px;
      }

      .status-active   { background: #e8f5e8; color: #2e7d32; }
      .status-found    { background: #fff7d1; color: #8c6d00; }
      .status-closed   { background: #ffebee; color: #c62828; }

      .detail {
        margin-bottom: 20px;
      }

      .detail strong {
        color: #2E5BBA;
        display: block;
        margin-bottom: 4px;
      }

      .photos {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
        gap: 15px;
        margin-top: 10px;
      }

      .photos img {
        width: 100%;
        border-radius: 10px;
        border: 2px solid #f0f0f0;
      }

      .actions {
        text-align: center;
        margin-top: 30px;
      }

      .btn {
        display: inline-block;
        background: #2E5BBA;
        color: white;
        padding: 10px 22px;
        border: none;
        border-radius: 50px;
        font-size: 1rem;
        text-decoration: none;
        cursor: pointer;
        transition: all 0.3s ease;
        margin: 0 8px;
      }

      .btn:hover {
        background: #1E4A9A;
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(46, 91, 186, 0.3);
      }

      .btn-secondary {
        background: #ff6b6b;
      }

      .btn-secondary:hover {
        background: #e85a5a;
      }

      .back-link {
        text-align: center;
        margin-top: 25px;
      }

      .back-link a {
        color: #2E5BBA;
        text-decoration: none;
        font-weight: bold;
      }

      .back-link a:hover {
        text-decoration: underline;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <h1>🔍 <%= @lost_item.item_type.capitalize %> Lost</h1>

      <div class="card">
        <p class="status status-<%= @lost_item.status %>">
          <%= @lost_item.status.capitalize %>
        </p>

        <div class="detail">
          <strong>Item Type:</strong>
          <p><%= @lost_item.item_type.capitalize %></p>
        </div>

        <div class="detail">
          <strong>Description:</strong>
          <p><%= @lost_item.description %></p>
        </div>

        <div class="detail">
          <strong>Location Lost:</strong>
          <p><%= @lost_item.location %></p>
        </div>

        <div class="detail">
          <strong>Date Lost:</strong>
          <p><%= @lost_item.lost_date.strftime("%B %d, %Y") %></p>
        </div>

        <div class="detail">
          <strong>Posted by:</strong>
          <p><%= @lost_item.user.email %></p>
        </div>

        <% if @lost_item.photos_array.any? %>
          <div class="detail">
            <strong>Photos:</strong>
            <div class="photos">
              <% @lost_item.photos_array.each do |url| %>
                <img src="<%= url %>" alt="Lost item photo">
              <% end %>
            </div>
          </div>
        <% end %>

        <% if logged_in? && current_user == @lost_item.user %>
          <div class="actions">
            <% if @lost_item.status == 'active' %>
              <%= button_to "Mark as Found",
                  mark_found_lost_item_path(@lost_item),
                  method: :patch,
                  class: "btn",
                  data: { turbo: false, turbo_confirm: "Confirm item has been found?" } %>

              <%= button_to "Close Listing",
                  close_lost_item_path(@lost_item),
                  method: :patch,
                  class: "btn btn-secondary",
                  data: { turbo: false, turbo_confirm: "Are you sure you want to close this listing?" } %>
            <% end %>
          </div>
        <% end %>
      </div>

      <div class="back-link">
        <%= link_to "← Back to Dashboard", dashboard_path %>
      </div>
    </div>
  </body>
</html>
