<!DOCTYPE html>
<html>
  <head>
    <title>All Lost Items - FoundIt</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
        background: #f5f7ff;
        color: #333;
      }
      .container {
        max-width: 1000px;
        margin: 0 auto;
        padding: 40px 20px;
      }
      h1 {
        text-align: center;
        color: #2E5BBA;
        margin-bottom: 30px;
      }
      .lost-items-list {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 25px;
      }
      .lost-card {
        background: white;
        border-radius: 15px;
        padding: 20px;
        box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        transition: transform 0.2s ease, box-shadow 0.2s ease;
        text-decoration: none;
        color: inherit;
      }
      .lost-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 18px rgba(46, 91, 186, 0.2);
      }
      .lost-card h3 {
        margin: 0 0 10px;
        color: #2E5BBA;
      }
      .lost-card p {
        margin: 4px 0;
        font-size: 0.95rem;
        color: #555;
      }
      .item-status {
        display: inline-block;
        padding: 4px 10px;
        border-radius: 12px;
        font-size: 0.8rem;
        font-weight: bold;
      }
      .status-active   { background: #e8f5e8; color: #2e7d32; }
      .status-found    { background: #fff7d1; color: #8c6d00; }
      .status-closed   { background: #ffebee; color: #c62828; }
    </style>
  </head>

  <body>
    <div class="container">
      <h1>🔍 <%= @viewing_all ? 'All Lost Items' : 'My Lost Items' %></h1>
      
      <% unless @viewing_all %>
        <div style="text-align: center; margin-bottom: 30px;">
          <%= link_to "View All Lost Items", all_lost_items_path,
                style: "display:inline-block; background:#2E5BBA; color:white; padding:12px 24px; border-radius:25px; text-decoration:none; font-weight:bold; transition:all 0.3s ease;" %>
        </div>
      <% else %>
        <div style="text-align: center; margin-bottom: 30px;">
          <%= link_to "View My Lost Items", lost_items_path,
                style: "display:inline-block; background:#6c757d; color:white; padding:12px 24px; border-radius:25px; text-decoration:none; font-weight:bold; transition:all 0.3s ease;" %>
        </div>
      <% end %>

      <% if @lost_items.any? %>
        <div class="lost-items-list">
          <% @lost_items.each do |item| %>
            <%= link_to lost_item_path(item), class: "lost-card" do %>
              <h3><%= item.item_type.capitalize %></h3>
              <p><strong>Description:</strong> <%= truncate(item.description, length: 80) %></p>
              <p><strong>Location:</strong> <%= item.location %></p>
              <p><strong>Date Lost:</strong> <%= item.lost_date.strftime("%b %d, %Y") %></p>
              <% if @viewing_all %>
                <p><strong>Posted by:</strong> <%= item.user.email %></p>
              <% end %>
              <span class="item-status status-<%= item.status %>">
                <%= item.status.capitalize %>
              </span>
            <% end %>
          <% end %>
        </div>
      <% else %>
        <div style="
          background: white;
          border-radius: 15px;
          padding: 40px 30px;
          text-align: center;
          color: #666;
          box-shadow: 0 5px 15px rgba(0,0,0,0.05);
          border: 1px solid #e0e0e0;
          max-width: 600px;
          margin: 0 auto 40px auto;
        ">
          <p style="font-size: 1.2rem; margin-bottom: 10px;">🔍 No lost items have been posted yet.</p>
          <p style="font-size: 0.95rem; margin-bottom: 20px;">No lost items to display at the moment.</p>
        </div>
      <% end %>

      <div style="text-align:center; margin-top:30px;">
        <%= link_to "← Back to Dashboard", dashboard_path,
              style: "color:#2E5BBA; text-decoration:none; font-weight:bold;" %>
      </div>
    </div>
  </body>
</html>
